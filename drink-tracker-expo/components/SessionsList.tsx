import React, { Component } from 'react';

import { DrinkingSession, NavigationProps } from '../commonTypes';

type SessionsListProps = {sessions: DrinkingSession[]} & NavigationProps;

const SessionList = (props: SessionsListProps) => {
    return (
        <></>

    );
};

export {SessionList};
